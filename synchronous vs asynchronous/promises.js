// promises
// Promises is the build  classes and constructor through which we can create the 
// promises that take 2 paramertes 
// parameterisse constructor 

// static methods methods meeas we can call it directly without creating the object 

// 1.Promises.all();

//2 type of the methods 
// static methods and instance methods 

// static methods we can call it directly by using className 
// example  
// promises have the static methods and instance methods 

// Promises

Promise.all();
Promise.allSettled();
Promise.any();
Promise.race();
Promise.reject();
Promise.resolve();
Promise.try();

// 2. instance methods   that call by object only 
// 1. then() : is the instance methods 
// 2. catch(): : this catches the error in the javascript
// 3. finally(): method



